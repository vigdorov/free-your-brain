export const enum TaskStatus {
    Progress = 'progress',
    Removed = 'removed',
    Done = 'done',
}

export const enum FolderType {
    Project = 'project',
    Information = 'information',
}

export enum PageType {
    Main = '',
    ChaosBox = 'chaos-box',
    Projects = 'projects',
    Information = 'information',
    Tags = 'tags',
    Calendar = 'calendar',
    Settings = 'settings',
    SigIn = 'sign-in',
}
