export enum ModalType {
    CreateTask = 'createTask',
    CreateFolder = 'createFolder',
    CreateTag = 'createTag'
}

export enum AddMenu {
    AddTask = 'addTask',
    AddFolder = 'addFolder',
    AddTag = 'addTag'
}
