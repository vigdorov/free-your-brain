export enum ModalType {
    CreateTask = 'createTask',
    CreateFolder = 'createFolder',
    CreateTag = 'createTag',
    CreateModal = 'addModal'
}

export enum AddMenu {
    AddTask = 'addTask',
    AddFolder = 'addFolder',
    AddTag = 'addTag'
}
